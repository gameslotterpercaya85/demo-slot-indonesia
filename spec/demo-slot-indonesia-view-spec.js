'use babel';

import DemoSlotIndonesiaView from '../lib/demo-slot-indonesia-view';

describe('DemoSlotIndonesiaView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
